meta {
  name: Get Map
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/map?day=1&period=1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  expect(res.status).to.equal(200);
  expect(res.body).to.be.an("array");
}
