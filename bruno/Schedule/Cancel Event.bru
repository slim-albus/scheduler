meta {
  name: Cancel Event
  type: http
  seq: 5
}

put {
  url: {{baseUrl}}/api/schedule/event/{{eventId}}/cancel
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

script:post-response {
  expect(res.status).to.equal(200);
  expect(res.body.status).to.equal("CANCELED");
}
